// Public entry for `@outerjs/server/storage` — object-store adapters and its type.
// Also re-exported from the package root for backward compatibility.
export { fromUnstorage, fromS3, memoryStorage } from "../storage";
export type { OuterStorage } from "../storage";

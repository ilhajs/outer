/**
 * Table index — grid lives in `+layout.tsx` (master–detail shell).
 * This page only fills the detail outlet slot when no record/new is open
 * (layout keeps the outlet hidden on this route).
 */
import ilha from "ilha";

export default ilha.render(() => "");
